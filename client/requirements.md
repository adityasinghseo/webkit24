## Packages
framer-motion | Essential for smooth, premium animations and micro-interactions
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
}
API Integration:
- Leads: POST /api/leads
- Growth Plan: POST /api/ai/growth-plan (returns JSON)
- Idea Generator: POST /api/ai/idea-generator (returns JSON)
